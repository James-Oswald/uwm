{
  "name": "unofficial-wynncraft-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "serve": "python3 -m http.server 4173",
    "update-cache": "node ./scripts/update-cache.mjs"
  }
}
